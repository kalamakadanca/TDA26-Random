services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tda26-api
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped

volumes: {}

networks:
  default:
    name: tda26-network